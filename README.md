# watermark-detection
 Developed a CNN-based image classification system to detect watermarks in digital images. Performed preprocessing, normalization, train-test splitting, model training, validation, and evaluation. Used convolution, pooling, batch normalization, and dropout layers, achieving reliable watermark detection accuracy.
